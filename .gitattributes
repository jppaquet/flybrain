# LF line endings in the repository; shell scripts must stay LF even on Windows/WSL
* text=auto eol=lf
*.sh text eol=lf

# Vendored three.js: excluded from GitHub's language stats, no diff of minified files
static/vendor/** linguist-vendored
*.min.js -diff
